In this project of 5 micro services we will be containerizing the different services with the help of a dockr compose for faster, safer and pruned configurations.
Esch services connects to a DB on the backend to temporay persist data to be used by the services depending on the type of data we will be making use of both sql and non sql DBs
Maximum security will be engrained right all the level of the dockerfile it service and healthchaecks will be set at regular intervals to make container dependency is taken into account
All our services are spring boot app